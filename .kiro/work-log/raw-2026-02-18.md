# 工作日志 2026-02-18

---

### 01:31

**问题：** 修复CI Docker镜像地址、品牌切换功能、Docker容器unhealthy导致的生产环境404，以及WordPress API运行时配置问题
**解决：** 修正CI镜像地址为正确的腾讯云HK仓库路径；简化品牌切换逻辑（移除config.js和域名匹配，改为BRAND_ID直接注入）；修复Docker healthcheck中localhost在Alpine无法解析的问题（改用127.0.0.1）；将WordPress API URL从Vite编译时变量改为Docker运行时注入（通过docker-entrypoint.sh注入window.**WORDPRESS_API_URL**）；WordPress API不可用时隐藏新闻模块；创建了/git-push和/summary两个工作流
**涉及文件：** `.github/workflows/ci-cd.yml`, `Dockerfile`, `docker-entrypoint.sh`, `index.html`, `src/services/httpClient.ts`, `src/views/xiading/XiadingHomePage.vue`, `docker-compose.yml`, `docker-compose.dev.yml`, `.agent/workflows/git-push.md`, `.agent/workflows/summary.md`

---
